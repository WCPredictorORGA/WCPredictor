server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA : renvoie index.html pour toutes les routes React
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache agressif sur les assets statiques (hachés par Vite)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
}
